import { AgruparPorPipe } from './agrupar-por.pipe';

describe('AgruparPorPipe', () => {
  it('create an instance', () => {
    const pipe = new AgruparPorPipe();
    expect(pipe).toBeTruthy();
  });
});
