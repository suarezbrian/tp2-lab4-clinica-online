import { ConvertirFechaDiaMesPipe } from './convertir-fecha-dia-mes.pipe';

describe('ConvertirFechaDiaMesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirFechaDiaMesPipe();
    expect(pipe).toBeTruthy();
  });
});
