import { FiltrosMisTurnosPipe } from './filtros-mis-turnos.pipe';

describe('FiltrosMisTurnosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrosMisTurnosPipe();
    expect(pipe).toBeTruthy();
  });
});
