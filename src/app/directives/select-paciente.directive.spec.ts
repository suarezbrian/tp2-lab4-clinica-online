import { SelectPacienteDirective } from './select-paciente.directive';

describe('SelectPacienteDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectPacienteDirective();
    expect(directive).toBeTruthy();
  });
});
